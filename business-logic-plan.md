# План реализации бизнес-логики: настоящее приложение (Redux Toolkit + локальная БД)

Пошаговый план превращения текущего UI (сейчас работающего поверх in-memory заглушки, теряющей всё при перезапуске) в полноценно рабочее однопользовательское офлайн-приложение на одном устройстве:

1. **Redux Toolkit** — реактивный слой состояния в памяти, заменяет самодельный `createStore`/`useStore` (`shared/lib/create-store.ts`, `shared/lib/use-store.ts`).
2. **expo-sqlite + Drizzle ORM** — настоящая локальная реляционная БД на устройстве, переживающая перезапуск приложения. Redux остаётся источником истины для UI (быстрые синхронные чтения, реактивность), SQLite — источником истины на диске (persistence поверх Redux, а не вместо него).
3. **Валидация форм и обработка ошибок записи** (Шаги 13–14) — закрывает открытые вопросы `docs/ux-guidelines.md` (молчаливый отказ форм при невалидных данных, незалогированные ошибки записи на диск), изначально оставленные без решения.
4. **Автоматические тесты и подготовка к релизу** (Шаги 15–16) — минимальный набор проверок и релизных артефактов, без которых результат остаётся работающим только на машине разработчика прототипом, а не рабочим приложением.

Технологический выбор по пунктам 1–2 зафиксирован: `AGENTS.md` («State management: Redux Toolkit», «Локальная персистентность: expo-sqlite + Drizzle»), `docs/app-overview.md` («Хранилище данных»), `docs/planned-features.md` (снят прежний запрет проектировать персистентность — теперь это явное решение, а не то, чего нужно избегать).

Ограничения, которым план не должен противоречить: `docs/data-constraints.md` (какие поля нельзя редактировать и почему — не зависит от способа хранения), `docs/planned-features.md` (offline-first синхронизация и совместное ведение семьёй — план ниже спроектирован так, чтобы не создавать для них архитектурных препятствий, но сам сетевой слой не реализует; это отдельный следующий этап, см. раздел «Что сознательно не входит» ниже).

## Архитектурные решения

### 1. Где живёт Redux-стор (снимает конфликт с правилом FSD «нижние слои не импортируют верхние»)

Слайсы (`createSlice`/`createEntityAdapter`) физически лежат в `entities/*/model` — это бизнес-логика. Сам `store`, типизированные `useAppDispatch`/`useAppSelector` и сборка редьюсера — в `shared/lib/store.ts`: это инфраструктура/wiring, а не бизнес-логика, поэтому по духу правила `shared: reusable, no business logic` это разрешено. `app/_layout.tsx` только оборачивает дерево в `<Provider store={store}>`, импортируя `store` из `shared/lib/store` (app → shared, разрешённое направление).

Сборка редьюсера: `shared/lib/store.ts` импортирует готовые редьюсеры из `entities/*/index.ts` (каждая сущность добавляет `accountsReducer`/`categoriesReducer`/`transactionsReducer` в свой публичный API) и собирает их через `combineReducers`. Формально это `shared`, импортирующий из `entities` — единственное осознанное отклонение от буквального правила направления импортов, принятое потому, что сборка стора — это композиция, а не бизнес-логика (аналогично тому, как `app/_layout.tsx` уже сегодня composes провайдеры из нижних слоёв). Более сложная альтернатива (`combineSlices` + ленивая инъекция, без этого импорта) даёт ту же практическую пользу только при большом числе слайсов, которых у нас всего три — усложнять ради чистоты правила не нужно (лишняя абстракция «про запас», см. `AGENTS.md` про lean code).

### 2. Где живёт локальная БД — тот же приём, что и со стором

- `shared/lib/db.ts` — открытие SQLite-соединения (`expo-sqlite`), сборка полной Drizzle-схемы (`drizzle(expo, { schema })`) и раннер миграций. По той же причине, что и `store.ts`, `shared` здесь импортирует определения таблиц из `entities/*/index.ts` (`accountsTable`, `categoriesTable`, `transactionsTable`) — то же принятое отклонение, что и для Redux, тем же обоснованием.
- Определение таблицы каждой сущности (`accountsTable = sqliteTable('accounts', {...})`) — в `entities/*/model/schema.ts`, рядом со слайсом. Столбцы 1-в-1 повторяют поля из `entities/*/model/types.ts`.
- Файлы миграций (генерируются `drizzle-kit generate`) — вне `src/`, в `drizzle/` в корне проекта (это артефакт тулинга, не код приложения — не часть FSD-слоёв).
- **Redux — единственный источник истины для UI, SQLite — write-through слой на диске:**
  - При старте приложения `shared/lib/db.ts` открывает БД, прогоняет миграции, читает все строки → этим гидрируется Redux-стор (один раз, до первого рендера экранов — см. Шаг 9).
  - Каждая мутация (`createAccount`, `updateTransaction`, …) синхронно диспатчит в Redux (UI обновляется мгновенно, как сегодня) и асинхронно пишет ту же мутацию в SQLite в фоне.
  - Это значит, что функции `create*`/`update*`/`archive*`/`delete*` **неизбежно становятся `async`/возвращают `Promise`** — если раньше это было временное упрощение in-memory заглушки, то настоящая запись на диск синхронной быть не может. Это единственное осознанное изменение контракта функций из `docs/app-overview.md` («сохранять сигнатуры функций как контракт») — meняются не параметры и не набор функций, а только `void → Promise<void>`. Список мест, вызывающих эти функции, нужно свести (Шаг 10) и убедиться, что ни один вызывающий код не полагается на синхронное завершение записи.

### 3. Судьба mock-данных

`entities/*/model/mock-data.ts` сегодня — это одновременно и «дефолтные данные приложения», и dev-заглушка. С настоящей БД это разные вещи:

- Реальное приложение должно стартовать **с пустой БД** у нового пользователя (0 счетов, 0 категорий, 0 операций) — это ожидаемое поведение настоящего финансового трекера, а не баг.
- `mock-data.ts` остаётся, но переквалифицируется в **дев-фикстуры**: используется только для сида БД в dev-режиме (`__DEV__`) или в будущих тестах, не попадает в продовую сборку как стартовые данные. Решить и явно закрепить на Шаге 8 — не переносить молча.

### 4. Настройки (тема/язык/последний счёт/черновик фильтров) — тот же паттерн, зеркально

Эти значения остаются на `createValueStore` (не переезжают в Redux — решение уже зафиксировано, см. `AGENTS.md`), но теперь тоже должны переживать перезапуск — иначе получится нелепо: операции сохраняются на диск, а тема каждый раз откатывается на дефолтную. Для них — не полноценная реляционная таблица (это не сущности с инвариантами и связями, а плоский набор пар ключ-значение), а **`expo-sqlite/kv-store`** — встроенный в `expo-sqlite` key-value слой с API, похожим на `AsyncStorage`, но поверх той же SQLite-БД (не тянет отдельную зависимость и отдельный файл БД). `createValueStore` дополняется чтением при инициализации и записью при `setValue` — тем же write-through приёмом, что и у сущностей.

## Шаги

### Шаг 0 — подготовка (сделано в этой сессии)
- [x] Зафиксировать выбор стека (Redux Toolkit, RTK Query на будущее, expo-sqlite + Drizzle ORM) в `AGENTS.md`, `docs/app-overview.md`, `docs/planned-features.md`.
- [x] Сохранить этот план в корне проекта.

### Шаг 1 — инфраструктура Redux-стора
- Установить `@reduxjs/toolkit`, `react-redux`.
- `shared/lib/store.ts`: `configureStore`, `combineReducers` из редьюсеров, экспортируемых `entities/*/index.ts`, типы `RootState`/`AppDispatch`, типизированные `useAppDispatch`/`useAppSelector`.
- `<Provider store={store}>` в `src/app/_layout.tsx`.
- Приёмка: приложение стартует с пустым стором, ни одна сущность ещё не мигрирована.

### Шаг 2 — сущность `account` на Redux (in-memory, без персистентности)
- `entities/account/model/use-accounts.ts`: `createSlice` + `createEntityAdapter<Account>()`, начальное состояние — пустое (данные появятся после Шага 9, не из `mockAccounts`).
- `useAccounts()`, `useAccount(id)`, `getAccount(id)`, `createAccount`, `updateAccount`, `archiveAccount` — сигнатуры такие же, как сегодня (async-версии — только с Шага 10, здесь пока синхронные, чтобы не смешивать два изменения в одном шаге).
- Ограничения `docs/data-constraints.md` не ослаблять: `updateAccount` по-прежнему без `currency`/`initialBalance` в `changes`.
- Приёмка: экран счетов работает на пустых/тестовых данных, вставленных вручную через консоль/dev-тулинг — визуальных регрессий нет.

### Шаг 3 — сущность `category` на Redux
- Аналогично Шагу 2: `createEntityAdapter<Category>()`, сортировка по `order` внутри `kind` в селекторе, `kind` по-прежнему неизменяем после создания.

### Шаг 4 — сущность `transaction` на Redux
- Аналогично: `createEntityAdapter<Transaction>()`, порядок «новые сверху» — через `sortComparer`/сортировку в селекторе, типизация `updateTransaction` остаётся `Partial<NewTransaction>` (см. `docs/data-constraints.md` про теоретическое ограничение `type`).

### Шаг 5 — производные вычисления как мемоизированные селекторы
- `computeAccountBalance`, `useFrequentCategories`, `useHomeSummary`, `useFeedItems`, `findMostFrequentTransferPair`, `sortAccountsByRole` — переводятся на `createSelector` (реэкспорт `reselect` из RTK) поверх Redux-стейта.
- Производные величины по-прежнему **не хранятся отдельным полем** — вычисляются на лету (принцип из `docs/planned-features.md`, sync-дружественность баланса).
- Приёмка: Главная, Лента, «умные» дефолты (`docs/ux-guidelines.md`, раздел I) — идентичны текущему поведению.

### Шаг 6 — dev-сид для ручного тестирования Redux-слоя
- Временно (до Шага 9) — dev-only способ наполнить пустой Redux-стор данными из `mock-data.ts` (например, диспатч `addMany` при старте в `__DEV__`), чтобы экраны можно было полноценно щупать руками, пока БД ещё не подключена.
- Явно пометить как временное — этот код убирается на Шаге 9, когда его заменяет настоящая гидрация из SQLite.

### Шаг 7 — схема Drizzle и миграции
- Установить `expo-sqlite`, `drizzle-orm`, `drizzle-kit` (dev-зависимость).
- `entities/account/model/schema.ts`, `entities/category/model/schema.ts`, `entities/transaction/model/schema.ts` — таблицы 1-в-1 с `types.ts` каждой сущности (транзакция — либо одна таблица с nullable-колонками под разные варианты `type`, либо явно решить на этом шаге, как дискриминированный union ложится на реляционную схему — зафиксировать выбор в этом файле, не откладывать).
- `shared/lib/db.ts`: собирает полную схему из `entities/*/index.ts`, открывает `expo-sqlite`, настраивает `drizzle()`.
- `drizzle-kit generate` → миграции в `drizzle/` (корень проекта), встроить прогон миграций при старте (`drizzle-orm/expo-sqlite/migrator` + Metro asset для папки миграций — сверить точный механизм с версией `expo-sqlite`/`drizzle-orm` на момент реализации, т.к. `AGENTS.md` требует читать версионные доки Expo перед написанием кода).
- Приёмка: пустая БД создаётся при первом запуске, миграции применяются без ошибок, схема видна через Drizzle Studio/CLI.

### Шаг 8 — сидирование дев-данных через настоящую БД
- Переквалифицировать `mock-data.ts` в дев-фикстуры: в `__DEV__` при пустой БД — одноразовая вставка `mockAccounts`/`mockCategories`/`mockTransactions` через Drizzle (`insert`) вместо диспатча напрямую в Redux, как было на Шаге 6.
- Продовая сборка стартует с пустой БД — это финальное поведение, явно зафиксировать (не «пока так», а осознанное решение реального приложения).
- Приёмка: `__DEV__`-сборка выглядит как раньше (данные есть), production-сборка стартует пустой (проверить через `expo run:ios --configuration Release` или явный флаг).

### Шаг 9 — гидрация Redux из БД при старте
- В `app/_layout.tsx` (или отдельном провайдере в `app/`) — до рендера основного дерева: открыть БД, прогнать миграции, прочитать все три таблицы, продиспатчить гидрацию в Redux-стор.
- Использовать уже подключённый `expo-splash-screen` (`SplashScreen.preventAutoHideAsync()`/`hideAsync()`) как точку удержания экрана загрузки на время гидрации — не изобретать отдельный спиннер/лоадер поверх уже имеющегося сплэша.
- Убрать dev-сид из Шага 6 (заменён настоящей гидрацией).
- Приёмка: холодный старт приложения показывает сплэш, затем реальные данные из БД; повторный запуск после закрытия приложения показывает те же данные, что были при выходе (ключевая проверка того, что персистентность реально работает).

### Шаг 10 — write-through персистентность и переход на `async`
- `createAccount`/`updateAccount`/`archiveAccount`, аналогично для `category`/`transaction`: после `dispatch` (мгновенное обновление UI) — асинхронная запись той же мутации в SQLite через Drizzle (`insert`/`update`). Функции становятся `async`, возвращают `Promise<void>`.
- Свести все вызовы этих функций по кодовой базе (`grep`) и проверить: ни один существующий вызывающий код не должен ломаться от того, что функция вернула `Promise` — там, где UX требует дождаться сохранения (например, закрытие модалки после сабмита), явно добавить `await`; там, где не требуется (мгновенный оптимистичный UI) — оставить fire-and-forget, но осознанно, а не случайно.
- Ошибка записи в SQLite на этом шаге минимально логируется (`console.error`, английский текст по конвенции проекта) и не блокирует уже применённое оптимистичное обновление Redux — полноценная обработка (решение, показывать ли что-то пользователю) вынесена в отдельный Шаг 14, чтобы не смешивать миграцию на `async` с проектированием UX ошибки.
- Приёмка: создание/редактирование/архивация/удаление в любой сущности переживают перезапуск приложения.

### Шаг 11 — настройки на write-through (`expo-sqlite/kv-store`)
- `createValueStore` дополняется: при инициализации — чтение сохранённого значения из `expo-sqlite/kv-store` (fallback на переданный `initialState`, если ключа ещё нет), при `setValue` — запись туда же в фоне.
- Затрагивает `change-theme` (тема), `change-language` (язык), `last-used-account-store`, `picked-category-store` (`add-transaction`), `feed-filters-store`. Не все из них обязаны переживать перезапуск в равной степени — например, черновик фильтров Ленты можно осознанно не персистить (это рабочий черновик, а не настройка) — решить по каждому явно, а не персистить всё скопом по инерции.
- Приёмка: тема/язык/последний счёт (и что ещё решено персистить) переживают перезапуск приложения.

### Шаг 12 — очистка и полная проверка
- Удалить `shared/lib/create-store.ts`/`shared/lib/use-store.ts`, если после Шагов 2–4 и 11 они больше нигде не используются.
- Полный ручной прогон (`run`): создание/редактирование/архивация счёта и категории, добавление/редактирование/удаление расхода/дохода/перевода, Главная, Лента с фильтрами, тема/язык — **с обязательным перезапуском приложения между шагами сценария**, чтобы подтвердить, что персистентность реально работает, а не просто не падает.
- Проверить границы FSD: слайсы и Drizzle-схемы — в `entities/*/model`, `store.ts`/`db.ts` — в `shared/lib`, `entities` не импортирует `app`, `shared/lib/store.ts`/`db.ts` не содержат бизнес-правил (валидация/архивация/фильтрация остаются в `entities`).

### Шаг 13 — Валидация полей форм и сообщения об ошибках
- Не имеет технической зависимости от Redux/SQLite-миграции (Шаги 1–12) — может выполняться в любой момент, вынесен сюда по порядку появления в этом плане, а не по обязательной последовательности.
- Закрывает открытый вопрос `docs/ux-guidelines.md`, раздел E: сейчас `handleSave` в `account-form-screen.tsx` и `category-form-screen.tsx` при пустом имени просто делает `return` — кнопка «Сохранить» не даёт никакой обратной связи.
- Решение: каждая форма получает локальное состояние ошибки поля (например `nameError: string | null`), выставляемое при попытке сохранить с невалидными данными, и показываемое рядом с полем — по HIG (Writing): по существу («Введите название»), без «Мы»/«We», без «ой!»/«упс!» (см. `docs/ux-guidelines.md`, разделы E и G).
- Затрагивает как минимум: `account-form-screen.tsx` (пустое имя), `category-form-screen.tsx` (пустое имя), `use-transaction-form.ts`/`use-edit-transaction-form.ts` (сумма ⩽ 0 или не выбран счёт/категория — сейчас `commit`/`save` просто возвращают `false` без сообщения), `use-transfer-form.ts`/`use-edit-transfer-form.ts` (`canSubmit` аналогично).
- Приёмка: попытка сохранить форму с пустым именем/нулевой суммой показывает конкретное сообщение у нужного поля вместо молчаливого отказа кнопки.

### Шаг 14 — Обработка ошибок записи в БД
- Зависит от Шага 10 (write-through должен уже существовать, чтобы было что обрабатывать).
- Закрывает открытый вопрос, оставленный на Шаге 10: ошибка записи в SQLite логируется (`console.error`) и дополнительно показывается пользователю ненавязчивым, не блокирующим уведомлением (конкретный компонент — тост/баннер — подобрать на этом шаге, следуя визуальному языку `docs/ux-guidelines.md`), поскольку Redux уже применил оптимистичное обновление и молчаливое расхождение UI/диска недопустимо для финансового приложения.
- Не откатывать оптимистичное обновление Redux при ошибке записи (откат добавил бы сложность мёржа состояний ради редкого случая — диск переполнен/повреждён) — только уведомление и лог, без автоматического retry-механизма (отдельная очередь повторов — вне рамок этого плана, YAGNI, пока не наблюдается как реальная проблема).
- Приёмка: искусственно вызванная ошибка записи (например, замоканный Drizzle `insert`, бросающий исключение, в тесте) не роняет приложение, попадает в лог и показывает пользователю уведомление.

### Шаг 15 — Автоматические тесты
- Инфраструктура: добавить `jest` + `jest-expo` (стандартный пресет для Expo-проектов) и `@testing-library/react-native` как dev-зависимости — в проекте сейчас нет ни одного тестового раннера.
- Покрыть тестами то, что сегодня сложнее всего проверить вручную и легче всего незаметно сломать:
  - `computeAccountBalance` (`entities/transaction`) — чистая функция, ключевая денежная логика.
  - Редьюсеры/селекторы сущностей после Шагов 2–5 (`createEntityAdapter`-слайсы, `useFrequentCategories`, `findMostFrequentTransferPair`, `sortAccountsByRole`, `useHomeSummary`, `useFeedItems`).
  - Ограничения `docs/data-constraints.md` на уровне редьюсеров (`updateAccount` не принимает `currency`/`initialBalance`, `updateCategory` не принимает `kind`) — тест, который поймает случайное ослабление этих ограничений при будущих правках.
  - Drizzle-схема (Шаг 7) — round-trip тест «записали через `insert` → прочитали через `select` → получили то же самое» на каждой таблице.
  - Write-through мутации (Шаг 10) и обработка ошибок (Шаг 14) — с замоканным `expo-sqlite`.
- Приёмка: новый скрипт `npm test` проходит локально и покрывает перечисленное; регрессия в любом из пунктов выше падает тестом, а не обнаруживается вручную в симуляторе.

### Шаг 16 — Подготовка к релизу
- Не связано с бизнес-логикой/персистентностью напрямую, но необходимо, чтобы результат был рабочим приложением, а не только рабочим у разработчика: проверить, что иконки и сплэш (`assets/expo.icon`, `assets/images/splash-icon.png`) актуальны после всех UI-правок.
- `bundleIdentifier` (`com.uvarovag.spend`) в `app.json` уже задан — добавить `eas.json` (сейчас отсутствует) с конфигурацией `build`/`submit`, если релиз пойдёт через EAS Build, либо явно зафиксировать альтернативный процесс (Xcode Archive вручную), если через него.
- Договориться о стратегии версионирования (`version`/`buildNumber` в `app.json`) перед первой публикацией.
- Минимальные материалы для App Store Connect: название, описание, скриншоты, ссылка на политику конфиденциальности (нужна даже для полностью офлайн-приложения без сети).
- Приёмка: `expo run:ios --configuration Release` собирается и ставится на физическое устройство; при наличии EAS — `eas build --platform ios` проходит без ошибок.

## Что сознательно не входит в этот план

По `docs/planned-features.md` (актуально даже после того, как локальная персистентность перестала быть «нельзя» и стала «сделано»):

- **Сетевой слой, RTK Query, синхронизация между устройствами и совместный (семейный) доступ.** Инструмент под sync зафиксирован (`docs/planned-features.md`), но `createApi`/эндпоинты, модели участника/семьи и сама синхронизация — это следующий этап, отдельный от текущего плана. SQLite здесь строго локальная БД одного устройства, не более.
- **UUID вместо `generateId`, поля `updatedAt`/`version`/`deviceId`/`memberId`.** Нужны только для будущей синхронизации между устройствами/участниками (см. пункт выше) — не добавлять «про запас» при проектировании схемы Drizzle; текущий `generateId` как `TEXT PRIMARY KEY` подходит.
- **Конвертация валют.** Не пробел, а зафиксированное архитектурное решение приложения (`docs/app-overview.md`) — суммы/балансы всегда в валюте своего счёта, агрегаты либо в разрезе одной валюты, либо списком по валютам. Этот план его не меняет и меняться не должен.
- **Веб-платформа.** Экраны построены на нативных SwiftUI-компонентах `@expo/ui` (`Picker`, `FieldGroup`, `DatePicker` и т.п.), у которых нет реализации под web — приложение не запускается интерактивно в браузере независимо от этого плана, и это не пробел персистентности, а более широкое, уже принятое архитектурное решение (iOS-first, Liquid Glass — см. `docs/ux-guidelines.md`). Замена нативного UI на кросс-платформенный — отдельная, значительно более крупная инициатива, вне рамок этого документа.
